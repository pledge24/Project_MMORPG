# 백로그

아직 착수하지 않은 작업 후보다. 우선순위 순으로 적는다.
지금 틀린 것은 `docs/tech-debt.md`가 담는다. 두 문서의 역할을 섞지 않는다.

---

## 1. Inventory 매핑 표 통합과 슬롯 입력 검증

**접근법이 폐기됐다(2026-09).** 부채 자체는 남아 있으므로 후보에서 빼지 않는다. 다시 잡을 때는
설계부터 새로 한다.

**왜 지금**: 클라가 보낸 슬롯 타입과 id가 검증 없이 `operator[]`와 `Mutable()`로 직행해
널 역참조가 가능하다. 손으로 유지되는 매핑 셋은 이미 한 번 사고를 낸 구조다. 둘은 같은 함수를
건드리므로 한 번에 끝난다. **그물이 이미 있다** — `InventoryTests.cpp`가 세 슬롯 타입을 전부
검사한다.

**선행 조건**: 미정. 폐기된 설계는 `docs/references/work/2026-09-10-inventory-cleanup.md`에 있다.

**완료 신호**: 미정.

---

## 2. 스택 상한 — 프로토콜 변경 연습

**왜 지금**: `DISABLED_StackDoesNotExceedMaxStack`이 이미 커밋되어 있고
`--gtest_also_run_disabled_tests`로 실제 빨강임이 확인됐다(미검증 스텁이 아니다).
`.proto` → 생성기 → 클라·서버 3곳 수정이 필요해 **"게임플레이 변경은 3곳"이라는
이 프로젝트의 기본 규약을 테스트가 있는 상태에서 처음 밟아보는** 연습이 된다.
규모가 작아 연습용으로 적절하다.

**선행 조건**: 없음. 1번을 먼저 하면 같은 파일을 두 번 열지 않는다.

**완료 신호**: `DISABLED_` 접두어 제거 후 종료 코드 0.
`ProtocolContractTests` 3개가 수정 없이 통과한다.

---

## 3. DummyClient 승격

인자화 → 실토큰 → 봇 상태머신 → 종료 코드.

**왜 지금**: L4의 재료다. 종료 코드를 갖게 되면 "종료 코드가 판정"이라는 규약에 그대로 편입된다.

**선행 조건**: 실토큰 단계에서 Redis와 AuthServer가 함께 떠 있어야 한다. L1보다 환경 의존이 크다.

**완료 신호**: 인자로 시나리오를 받아 실행하고 실패 시 0이 아닌 코드로 종료한다.

---

## 4. CI — 게임 서버와 UE 클라 티어

인증 서버 티어와 규범 검사가 `.github/workflows/ci.yml`에 들어갔다. 규범 검사는 빌드 없이 도는
텍스트 검사라 러너 제약을 받지 않는다(`Tools/ConventionLint/`, #53). 남은 두 티어는 **호스티드
러너에서 돌지 않는다.**

**무엇이 막는가** (2026년 9월 17일 실측)

| 티어 | 막는 것 |
| --- | --- |
| 게임 서버 | `.vcxproj`의 `PlatformToolset`이 `v145`다. GitHub `windows-2025` 러너의 Visual Studio는 2022(17.14)이고 툴셋은 `v143`까지다 |
| UE 클라 | UE 5.8 런처 설치본이 필요하다. 러너에 엔진이 없다 |

**선행 조건**: 아래 두 방향 중 하나를 사람이 정한다.

1. **self-hosted 러너를 이 머신에 등록한다.** 두 티어가 함께 풀리는 유일한 방법이다.
   **다만 이 저장소는 공개다.** 포크에서 온 PR이 이 머신에서 코드를 실행하게 되므로,
   워크플로 트리거를 어디까지 열지부터 정해야 한다. 머신이 켜져 있어야 한다는 제약도 있다.
2. **게임 서버만 호스티드로 옮긴다.** `PlatformToolset`을 `v143`으로 내린다. 로컬 개발 환경이
   VS 18이므로 `docs/build.md`의 빌드 경로와 함께 판단한다. **UE 클라는 이 방법으로 풀리지
   않는다.**

**완료 신호**: 푸시나 PR에서 세 티어가 자동으로 돌고 실패 시 빨간 체크가 뜬다.

---

## 5. UE L2 Automation Test

**왜 지금**: L1(LLT)은 폐기됐지만 L2는 별도 타깃이 필요 없어 설치본에서 바로 된다
(`IMPLEMENT_SIMPLE_AUTOMATION_TEST`가 설치본 `Core/Public/Misc/AutomationTest.h:4297`에 존재).

**선행 조건**: 없다. 사람 손이 필요하다고 적어 두었던 제약이 풀렸다.

**실행 경로가 실측으로 확인됐다.** `P1/Scripts/Run-UeTests.ps1`이 `UnrealEditor-Cmd`로
에디터를 띄우지 않고 돌린다. 리포트의 `index.json`을 읽어 판정하고 스스로 종료 코드를
낸다. 벽시계 시간은 14.5초와 47.3초로 측정했다. 떠 있는 에디터와 충돌하지 않는다.

**종료 코드만 보면 안 된다.** `UnrealEditor-Cmd`는 매칭된 테스트가 하나도 없어도 `0`을
돌려준다. 필터 오타가 그대로 초록으로 보인다. 위 스크립트가 이 구멍을 막는다.

착수 단계: `P1/Source/P1/Tests/`에 테스트 1개 → `Build.bat`으로 에디터 타깃 빌드
(`docs/build.md` 「빌드 명령」, **에디터를 닫고 돌린다**) → `Run-UeTests.ps1`.

첫 테스트는 빨강으로 먼저 쓴다. 테스트가 실제로 실패할 때 `failed` 필드가 올라가는지
아직 재지 못했고, 그 자리에서 함께 측정된다.

대체 실행 경로 둘은 남겨 둔다. 에디터가 이미 떠 있을 때 프로세스 시작 비용을 치르지 않는
언리얼 MCP의 `RunTests`와, 사람이 쓰는 에디터의 `Window > Test Automation`이다.

첫 대상 후보는 이동 보간 수식(`AP1Creature`의 `MoveQueue`) 정도다. 클라 쪽에 남은 순수 로직이
얇다는 점도 함께 고려한다.

**완료 신호**: `Run-UeTests.ps1`이 이 저장소의 테스트 1개에 종료 코드 `0`을 낸다.

---

## 6. 장착 슬롯과 퀵 슬롯의 매핑 대상 결정

**왜 지금**: `SLOT_TYPE_EQUIPPED`와 `SLOT_TYPE_QUICK`은 프로토콜에 정의돼 있으나 인벤토리의
`slotTypeToItemTypeMappings`에는 없다. #25가 이 둘을 크래시 없는 거부로 고정했고, #27이
의도적 제외로 `InventoryTests.cpp`의 기대 집합에 못박았다. 지금 남은 것은 **둘을 어떤 아이템
타입에 매핑해야 옳은지를 정하는 설계 판단 하나**뿐이라 범위가 선명하다.

**선행 조건**: 장착 슬롯을 `EquippedGear`가 계속 단독으로 관리할지 정한다. 퀵 슬롯은 서버에
저장소 자체가 없으므로 무엇을 어디에 저장할지부터 정한다. 1번과 같은 파일을 건드리므로 두
작업을 묶으면 파일을 두 번 열지 않는다.

**완료 신호**: 두 슬롯 타입이 매핑 표에 들어가거나, 넣지 않는 이유가 `docs/ARCHITECTURE.md`에
적힌다. 어느 쪽이든 `InventoryTests.cpp`의 기대 집합을 함께 갱신해 테스트가 초록으로 남는다.

---

## 7. 생성 파이프라인과 데이터 계약

폴더 구조와 컨벤션 이행(#38)에서 유예한 넷이다. 근거는
`docs/work/2026-09-19-convention-migration.md`의 B절과 L절에 있다.

| ID | 무엇을 바꾸는가 |
| --- | --- |
| `B3` | 생성 스크립트를 `GenPackets.bat`에서 `Protocol/Generate.ps1`로 바꾼다 |
| `B7` | `.proto` 스키마 3개(`Enum`·`Struct`·`Protocol`)를 도메인 단위로 나눈다 |
| `L1` | 기획 수치 원본을 `.xlsx` 바이너리에서 CSV 또는 JSON으로 바꾼다 |
| `L3` | `DataVersion` 핸드셰이크 검증을 넣는다 |

**왜 유예했나**: `B3`은 `.bat`이 cp949이고 6곳 배포가 검증된 경로다. 이름과 언어를 함께 바꾸면
이행에서 검증할 크기를 넘었다. `L1`은 `ExcelToJsonConverter.py`의 입력 형식을 바꾸는 기능
변경이다. `B7`과 `L3`은 표에만 유예로 판정되어 있고 계획 문서가 근거를 따로 적지 않았다.

**선행 조건**: `B3`과 `B7`은 둘 다 생성기를 건드리므로 함께 하면 파이프라인을 한 번만 검증한다.
나머지는 미정.

**완료 신호**: 미정.

---

## 8. 빌드 타깃과 솔루션 구성

같은 이행에서 유예한 셋이다. 근거는 계획 문서의 C절과 K절에 있다.

| ID | 무엇을 바꾸는가 |
| --- | --- |
| `C9` | `P1Editor.Target.cs`만 있고 없는 `P1Editor` 모듈을 만든다 |
| `C10` | 클라이언트 전용 타깃 `P1Client.Target.cs`를 만든다 |
| `K4` | `AuthServer.esproj`를 `Server.sln`에 등록한다 |

**왜 유예했나**: 계획 문서가 셋 다 표에만 유예로 판정하고 근거를 따로 적지 않았다. 셋 모두
지금 없어도 빌드와 실행이 도는 자리다.

**선행 조건**: `C9`는 10번(에디터 에셋 검증기)의 자리가 되므로 그쪽을 착수하면 함께 필요해진다.

**완료 신호**: 미정.

---

## 9. CommonUI 도입

같은 이행에서 유예한 둘이다. 근거는 계획 문서의 F절과 `docs/conventions.md` 2.15에 있다.

| ID | 무엇을 바꾸는가 |
| --- | --- |
| `F2` | CommonUI 플러그인을 켠다 |
| `F3` | `UP1UserWidget`과 `UP1ActivatableWidget` 두 계층으로 나눈다 |

**왜 유예했나**: 둘을 유예하는 대신 공용 베이스 클래스(`F1`)를 먼저 했다. 도입할 때
`UP1UserWidget`의 부모만 `UCommonUserWidget`으로 바꾸면 되도록 자리를 만들어 둔 것이다(#51).

**선행 조건**: 없다. **두 계층을 하나로 합치려는 시도는 하지 않는다.**
`UCommonActivatableWidget`이 `UCommonUserWidget`을 상속하므로 베이스 하나가 두 계층의 부모가 될
수 없다(`conventions.md` 2.15).

**함께 풀리는 것**: `docs/tech-debt.md`의 「캐릭터 클래스와 컨트롤러에 관심사가 뭉쳐 있다」가
담고 있는 위젯 수명 관리가 여기서 정리된다. 지금은 `AP1InGamePlayerController`가 7종을 손으로
여닫는다.

**완료 신호**: 미정.

---

## 10. 서버 게임 로직 폴더 분할

같은 이행에서 유예한 둘이다. 근거는 계획 문서의 I절에 있다.

| ID | 무엇을 바꾸는가 |
| --- | --- |
| `I6` | 전투를 `Room.cpp`와 `Monster.cpp`에서 꺼내 `Combat/`에 둔다 |
| `I7` | AI를 `Monster.cpp`에서 꺼내 `AI/`에 둔다 |

**왜 유예했나**: 파일을 쪼개는 기능 변경이다. 폴더를 옮기는 이행과 성격이 다르다. 목표 위치는
`docs/folder-structure.md`가 이미 적고 있다.

**선행 조건**: `Room`이 갓 클래스라는 부채와 같은 파일을 건드린다
(`docs/tech-debt.md`의 「`Room` / `DBRequestFunctions` 갓 클래스」). 어느 쪽을 먼저 할지 정한다.

**완료 신호**: 미정.

---

## 11. 에디터 에셋 검증기

같은 이행에서 유예한 하나다. 근거는 계획 문서의 M절에 있다.

| ID | 무엇을 바꾸는가 |
| --- | --- |
| `M5` | `P1Editor/Validation/`에 에셋 검증기를 만든다 |

**왜 유예했나**: 계획 문서가 표에만 유예로 판정하고 근거를 따로 적지 않았다.

**선행 조건**: 8번의 `C9`가 먼저다. 지금 `P1Editor` 모듈 자체가 없다.

**무엇이 남는가**: 텍스트로 판정할 수 있는 규범은 #53이 CI로 옮겼다
(`Tools/ConventionLint/`). 여기 남는 것은 에셋을 열어야 알 수 있는 것이다. 텍스처 그룹과 압축
설정, 참조 경계, 머티리얼 인스턴스의 부모 같은 것이 후보다.

**완료 신호**: 미정.

---

## 12. `Content/External/`의 보호 수단 결정

**왜 지금**: LFS 전환이 끝나 `Content/P1/` 501개는 `git checkout`이 되돌린다. 그런데
`Content/External/` 2,924개 5.2GB는 추적 대상이 아니라 `P1/Scripts/Backup-UeContent.ps1`의
스냅샷이 유일한 되돌리기 경로로 남았다. **그 스크립트의 주석은 「`docs/backlog.md` 7번의 LFS
전환이 끝나면 이 스크립트는 버린다」고 적고 있다.** 전환은 #52가 끝냈으므로, 주석을 그대로 두면
다음 세션이 근거가 충족됐다고 읽고 보호를 없앤다.

훅의 경로 가드는 에이전트의 쓰기만 막는다. 사람이 에디터에서 지우는 것은 막지 못한다.

**선행 조건**: 없다. 다른 항목보다 먼저 정해도 된다.

**완료 신호**: 스크립트를 유지하기로 하면 주석이 유지하는 이유를 적는다. 버리기로 하면 보관소를
무엇이 대신 지키는지 적힌다. 어느 쪽이든 「LFS 전환이 끝나면 버린다」는 문장이 사라진다.

---

## 13. `P1/PSD/`와 `P1/PNG/` 삭제

**왜 지금**: 175개 36MB가 ClassicMMOUI 팩을 잘못된 경로로 받은 부산물이다. 2026년 9월 22일에
전수 대조했다. PNG 168개는 `Content/External/ClassicMMOUI/Textures/` 아래 `.uasset`과 1대 1로
대응하고, 이름이 다른 둘도 대응 상대를 찾았다(`icon.png`가 `iconHud`와 `iconHero`다). PSD 7개는
`Content/External/ClassicMMOUI/UI/`의 `.uasset` 7개와 이름이 대응한다.

`P1/.gitignore`의 `PNG/`와 `PSD/` 두 줄이 이 폴더들을 막고 있어 추적되지 않는다. 자작이 아니므로
팩 재설치로 복구된다.

**선행 조건**: #81이 끝나야 한다. `TextureTools.import_file`이 디스크의 이미지를 텍스처 에셋으로
임포트하므로, UI 텍스처를 팩에서 복사하는 대신 `P1/PNG/`에서 새로 임포트하는 경로가 있다. 지금
지우면 그 경로가 닫힌다.

**완료 신호**: 두 폴더가 사라지고 `P1/.gitignore`의 해당 두 줄이 함께 지워진다.

---

## 14. UE MCP 관련 결정을 ADR-0003으로 옮기기

**왜 지금**: 2026년 9월 17일 리포트의 「머지 전에 정해야 하는 것」에 「Epic의 스킬 플러그인을
쓸 것인가」가 적혀 있었다. **그 판단이 규범 문서로 옮겨지지 않았다.** `docs/reports/`는 git이
추적하지 않으므로 저장소에 남지 않았고, 9월 22일 세션이 그것을 모른 채 플러그인이 이미 답해 둔
문제를 세 시간에 걸쳐 다시 겪었다. CLAUDE.md가 「리포트에만 적으면 저장소에 남지 않는다」고
경고한 그대로다.

옮길 것이 셋이다.

| 무엇 | 지금 어디 있는가 |
| --- | --- |
| Epic 스킬 플러그인 도입 판단 | 어디에도 없다. 2026년 9월 22일에 설치했다 |
| 네 층 판정 구조와 보관소 경로 가드 | 훅 코드의 주석에만 있다 |
| **UE MCP 작업 전에 `AgentSkillToolset.ListSkills`를 부른다** | 어디에도 없다 |

셋째가 특히 중요하다. 엔진 내장 `AgentSkillToolset`에 `BlueprintBasicsSkill`과
`MaterialBasicsSkill`이 들어 있고 조회 도구는 처음부터 열려 있었는데, **그 존재가 어디에도 적혀
있지 않아서 아무도 부르지 않았다.** ADR-0003의 허용 표에는 도구 이름만 있고 무엇에 쓰는지가 없다.

**선행 조건**: 없다.

**완료 신호**: ADR-0003만 읽어도 현재 통제 구조와 스킬 사용 규약을 알 수 있다. 리포트를 찾아
읽지 않아도 된다.

---

## 하지 않기로 확인된 것

다시 제안하지 않는다. 근거는 각 문서에 있다.

| 안 하는 것 | 근거 |
|---|---|
| UE L1 (Low-Level Tests) | `docs/testing.md` 「UE L1(Low-Level Tests)은 채택하지 않는다」 |
| 엔진 소스 패치 | `docs/build.md` 「엔진 제약」 |
