# 내부 활용 문서 — 외부에 공개할 md만 추적한다(README 등).
# 의도적으로 공유해야 할 때만 git add -f
CLAUDE.md

# 내부용 문서·자료 폴더. 루트 기준으로만 제외하므로
# 하위 모듈에 Docs 를 두면 그건 정상 추적된다.
/Docs/
/Ref/

# AI 도구 설정(규칙·스킬 등). 개발자 개인 환경이라 어느 위치든 제외.
.claude/
