# 훅 차단 카운터 — 머신 로컬 통계. 커밋마다 diff가 끼면 잡음이 되고,
# 브랜치를 오갈 때 합쳐질 수 없는 성질의 파일이다. 백업 대상에는 포함할 것.
.claude/hooks/block_counter.log

# 언리얼 MCP 감사 로그 — 통과시킨 호출의 기록. 위와 같은 이유로 추적하지 않는다.
# 사고가 났을 때 되짚는 용도이므로 백업 대상에는 포함할 것.
.claude/hooks/ue_audit.log

# 걷어낸 하네스 v1 보관 — 로컬로 옮길 예정이라 추적하지 않는다.
custom-harness/
