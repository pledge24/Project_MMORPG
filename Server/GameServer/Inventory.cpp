#include "pch.h"
#include "Inventory.h"

Inventory::Inventory()
{
    //ASSERT_CRASH(categorySlots.size() == categoryN);
    //
    //gear.resize(categorySlots[0]);
    //gearDirtyFlags.resize(categorySlots[0]);

    //consumables.resize(categorySlots[1]);
    //consumablesDirtyFlags.resize(categorySlots[1]);

    //miscellaneous.resize(categorySlots[2]);
    //miscellaneousDirtyFlags.resize(categorySlots[2]);
}

Inventory::~Inventory()
{
}

void Inventory::Init()
{
}
