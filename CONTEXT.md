# Project_MMORPG

UE5 클라이언트와 자체 IOCP C++ 게임 서버, Node 인증 서버로 이루어진 3티어 MMORPG다. 이 문서는
세 티어가 공유하는 낱말을 정한다. 구현과 배치는 담지 않는다.
— 구현은 `docs/ARCHITECTURE.md`가, 배치는 `docs/folder-structure.md`가 담는다.

## Language

### 동기화 대상

**엔티티(Entity)**:
게임 서버가 룸 안에서 상태를 들고 클라이언트에 복제하는 대상이다. 캐릭터가 아닌 것도 들어올 수
있는 자리다.
_피할 말_: 오브젝트, Object

**크리처(Creature)**:
체력과 위치를 갖고 스스로 움직이는 엔티티다. 플레이어와 몬스터의 공통 부모다.

**플레이어(Player)**:
접속한 사람의 계정에 묶인 크리처다.
_피할 말_: 캐릭터(생성과 삭제의 대상을 가리킬 때만 「캐릭터」를 쓴다), 유저

**내 플레이어(MyPlayer)**:
클라이언트에서 그 클라이언트가 조종하는 플레이어다. 같은 화면의 다른 플레이어와 입력 처리가
다르므로 별도의 낱말을 둔다.
_피할 말_: 로컬 플레이어, 내 캐릭터

**몬스터(Monster)**:
행동을 게임 서버가 결정하는 크리처다. 클라이언트는 결과 상태만 받는다.
_피할 말_: 미니언, NPC

### 공간

**룸(Room)**:
게임 서버의 동시성 단위이자 공간 단위다. 룸이 소유한 상태는 그 룸의 큐 위에서만 바뀐다.
_피할 말_: 월드, 존, 채널

**셀(Cell)**:
룸 안을 일정 크기로 자른 격자 한 칸이다. 브로드캐스트 범위와 근접 탐색의 단위다.
_피할 말_: 그리드, 타일, 섹터

**맵(Map)**:
클라이언트가 로드하는 레벨이다. 룸과 일대일이 아니다. 지금은 여러 룸이 한 맵 안의 논리 분할로
들어 있다.
_피할 말_: 레벨, 스테이지

### 티어와 계약

**인증 서버(AuthServer)**:
계정과 로그인을 담당하는 Node 티어다. `UserDB`를 소유한다.

**게임 서버(GameServer)**:
게임 규칙을 담당하는 C++ 티어다. `GameDB`를 소유한다.

**액세스 토큰(Access Token)**:
인증 서버가 로그인 성공 시 발급해 Redis에 넣는 UUID다. 인증 티어와 게임 티어를 잇는 유일한
값이다.
_피할 말_: 세션 토큰, JWT, 인증 키

**패킷(Packet)**:
`.proto` 스키마에서 생성된 메시지다. 클라이언트와 게임 서버를 잇는 유일한 계약이다. `C_`는
클라이언트에서 서버로, `S_`는 서버에서 클라이언트로 가는 것을 뜻한다.
_피할 말_: 메시지, RPC, 이벤트

### 상태

**ground state**:
잃으면 복구할 수 없는 상태다. `UserDB`와 `GameDB`의 행, 그리고 Redis의 액세스 토큰이 여기
해당한다.

**derived state**:
ground state에서 다시 만들 수 있는 상태다. 룸 안 엔티티의 런타임 상태가 여기 해당한다. 진실의
원천으로 삼지 않는다.
_피할 말_: 캐시, 임시 상태

### 소지품

**아이템(Item)**:
플레이어가 소지하거나 착용할 수 있는 것의 정의다. 수치는 `DesignData/`의 기획 원본에서 온다.

**인벤토리(Inventory)**:
플레이어가 소지한 아이템의 목록이다. 착용 여부와 무관하다.
_피할 말_: 가방, 소지품

**장비(EquippedGear)**:
플레이어가 착용 중인 아이템이다. 인벤토리와 별개의 자리를 갖는다.
_피할 말_: 장착 아이템, 착용 슬롯
