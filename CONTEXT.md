# Project_MMORPG

3티어 MMORPG다. 언리얼 클라이언트와 C++ 게임 서버가 같은 게임 규칙을 양쪽에서 다루고, Node
인증 서버가 로그인을 맡는다. 이 문서는 세 티어가 공유하는 낱말만 담는다. 폴더 배치는
`docs/folder-structure.md`, 되돌리기 어려운 결정은 `docs/adr/`에 있다.

## 용어

**엔티티** (`Entity`):
서버가 상태를 쥐고 클라이언트에 동기화하는 개체. 지금은 플레이어와 몬스터가 전부이지만,
투사체처럼 캐릭터가 아닌 동기화 대상도 여기 속한다.
쓰지 않는 말: 오브젝트, 캐릭터

**룸** (`Room`):
맵 하나에 대응하는 서버의 런타임 단위. 그 맵 위의 엔티티와 판정을 담으며, 잡 큐를 상속하므로
룸이 소유한 상태는 큐 위에서 직렬화된다.
쓰지 않는 말: 월드, 존

**게임 도메인**:
게임 규칙에 속하는 코드. 그 기능을 들어낼 때 함께 지워지는 것이 게임 도메인이고, 남는 것은
서버를 돌리거나 화면을 그리는 배선이다.
